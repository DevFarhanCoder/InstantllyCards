# .easignore - Files to exclude from EAS builds
# (This overrides .gitignore for EAS Build uploads)

# Allow google-services.json to be uploaded to EAS
# even though it's in .gitignore
!google-services.json

# Exclude large files and folders to reduce build size
*.md
*.log
*.aab
*.apk
.git/
.vscode/
node_modules/.cache/
**tests**/
*.test.ts
*.test.tsx
*.spec.ts
*.spec.tsx

# Use CNG / Prebuild - exclude android folder so EAS regenerates it
android/

# Use local android folder (already has expo-notifications removed)
