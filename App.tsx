// Minimal Expo entry for projects using Expo Router
// Metro expects to import `App` from the project root (../../App in expo/AppEntry).
// Forward the root to the expo-router entry which handles the routing/layouts.
import 'expo-router/entry';

// Note: no default export required — expo-router/entry registers the root component.
